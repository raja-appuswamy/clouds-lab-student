# Student-side autograding CI for Phase 6.
#
# The offline tests parse terraform/*.tf and agent/policy.json
# (python-hcl2 — no terraform binary, no GCP credentials). The live tests curl the Terraform-managed
# service while it exists and skip once your report records the destroy. The report tests read
# submission/phase6_report.json, built stage by stage by make_report.py.
name: autograde-phase-6

on:
  push:
    branches: ["**"]
  workflow_dispatch: {}

# Cancel a superseded run when you push again — the latest push is what gets graded.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  autograde:
    runs-on: ubuntu-latest
    steps:
      - name: Check out submission
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt -r phase-6-capstone/requirements.txt

      - name: Run autograder tests (Terraform static checks, live service, report)
        run: >
          python -m pytest phase-6-capstone/tests
          -p autograder.points
          --results-json results.json
          -q

      - name: Upload score artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase-6-score
          path: results.json
