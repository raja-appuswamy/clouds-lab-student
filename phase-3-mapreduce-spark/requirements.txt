# Phase 3 dependencies — one file, three consumers:
#   * the mr-worker Cloud Run function (gcloud reads this at deploy time),
#   * run_mr.py in Cloud Shell (drives the workflow),
#   * the autograder in CI (reads your Parquet + wordcount.json).
# NO pyspark: Spark runs only in Colab (the notebook installs it there).
functions-framework>=3.5,<4
google-cloud-storage>=2.14,<4
google-cloud-workflows>=1.14,<2
pyarrow>=14
